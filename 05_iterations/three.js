
const obj = {
    js: "javascript",
    cpp: "c++",
    rb: "ruby"
}

for (const key in obj) {
    console.log(`${key} shortform is for ${obj[key]}`);
    }

const languages = ["js", "cpp", "python"]

for (const key in languages) {
    console.log(languages[key]);    
    }

